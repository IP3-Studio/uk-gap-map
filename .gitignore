# dependencies & build artifacts
node_modules/
.next/
out/
next-env.d.ts
*.tsbuildinfo

# OS
.DS_Store

# local research materials (not published)
*.pdf
book-text.txt
book-splits/

# internal working/provenance files (not needed to build; may contain local paths)
data/raw/trello-candidates.json
data/raw/lens-extraction.json

# internal handoff / working state (kept local, not published)
HANDOFF.md

# local editor / preview config
.claude/
